<br/><br/><br/>
<div class="container">

  <h2>Administration</h2><hr><br/>

  <table class="table table-striped">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Prénom</th>
        <th scope="col">Nom</th>
        <th scope="col">Email</th>
        <th scope="col">Date d'inscription</th>
        <th scope="col"></th>
        <th scope="col"></th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
      <% @all_users.each_with_index do |user,index| %>
        <tr>
          <th scope="row"><%= index+1 %></th>
          <td><%= user.first_name %></td>
          <td><%= user.last_name %></td>
          <td><%= user.email %></td>
          <td><%= user.created_at %></td>
          <td><%= link_to 'Voir plus' , admin_user_path(user.id) %></td>
          <td><%= link_to 'Modifier' , edit_admin_user_path(user.id) %></td>
          <td><%= button_to "Supprimer", admin_user_path(user.id) , method: :delete, data:{confirm: "Supprimer ?"} , class:"btn btn-danger" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</div>

